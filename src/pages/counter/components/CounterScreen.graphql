query CounterScreen {
  counter {
    id
    user_id
    count
  }
}

mutation UpdateCounter($input: counter_set_input) {
  update_counter(where: {}, _set: $input) {
    returning {
      id
      user_id
      count
    }
  }
}
